var express = require('express');
var mysql = require('mysql');
var app = express();

var dbConnection = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'shantanu1'
});

dbConnection.connect(function(error){
    if(!!error){
        console.log("Failed to connect to the database");
    }
    else{
        console.log("Connected to the database");
    }
});


app.get("/getAllItems",function(req,res){
    dbConnection.query("SELECT * FROM items", function(error,rows,fields){

        if(!!error){
            console.log("Failed to fetch records from table");
        }
        else{
            console.log("Got the records");
        }

    });
});

app.listen(1337);